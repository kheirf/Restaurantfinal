<%= form_for(@subscribe) do |f| %>
  <% if @subscribe.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(@subscribe.errors.count, "error") %> prohibited this subscribe from being saved:</h2>

      <ul>
      <% @subscribe.errors.full_messages.each do |msg| %>
        <li><%= msg %></li>
      <% end %>
      </ul>
    </div>
  <% end %>
  <div class="formContainer">
  	<div class="field">
    	<%= label_tag "Name" %>
    	<%= f.text_field :customerName %>
  	</div>
  	<div class="field">
    	<%= label_tag "Contact Number" %>
    	<%= f.text_field :contactNo %>
  	</div>
  	<div class="field">
    	<%= label_tag "Email Address (used as login)" %>
    	<%= f.text_field :email %>
  	</div>
  	<div class="field">
    	<%= label_tag "Password" %>
    	<%= f.text_field :password %>
  	</div>
  	<div class="actions">
    	<%= f.submit %>
  	</div>
  </div>
<% end %>
